# Config nginx minimale pour le challenge ACME (avant SSL)
events { worker_connections 64; }
http {
    server {
        listen 80;
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        location / { return 200 "ok"; }
    }
}
