events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    gzip          on;
    gzip_types    text/plain text/css application/json application/javascript text/xml;

    # Redirect HTTP → HTTPS
    server {
        listen 80;
        server_name YOUR_DOMAIN;
        return 301 https://$host$request_uri;
    }

    # Résolveur DNS Docker — force la re-résolution dynamique des upstreams
    resolver 127.0.0.11 valid=10s ipv6=off;

    server {
        listen 443 ssl;
        server_name YOUR_DOMAIN;

        ssl_certificate     /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        # Headers sécurité
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-Content-Type-Options "nosniff";
        add_header Referrer-Policy "strict-origin-when-cross-origin";

        # Frontend Vue.js — variables pour forcer la re-résolution DNS
        set $frontend_upstream http://frontend:80;
        location / {
            proxy_pass         $frontend_upstream;
            proxy_set_header   Host $host;
        }

        # API FastAPI — variables pour forcer la re-résolution DNS
        set $app_upstream http://app:8000;
        location ~ ^/(api|auth|health)/ {
            proxy_pass         $app_upstream;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            # Timeout long pour les batchs Gmail
            proxy_read_timeout 120s;
        }
    }
}
